unbindall
bind mouse_x                    "yaw"
bind mouse_y                    "pitch"
bind mouse1 					"+attack"
bind mouse2 					"+attack2"

bind w                          "+forward"
bind a 							"+left"
bind d 							"+right"
bind s 							"+back"
bind ctrl 						"+duck"

//bh alias
alias +jump_ "exec "+jump"
alias -jump_ "exec "-jump"
bind mwheelup "+jump_"

bind shift 						"+sprint"
bind alt 						"switchhands"
alias +qsw "slot3"
alias -qsw "lastinv"
bind q +qsw

bind b 							"buymenu"
bind r 							"+reload"
bind e 							"+use"
bind g 							"drop"
bind j 							"+spray_menu"
bind f 							"+lookatweapon"
bind i 							"show_loadout_toggle"
bind t							"+spray_menu"
bind ` 							"toggleconsole"
bind tab 						"+showscores"
bind z							"radio"
bind mouse3 					"player_ping"
bind enter 						"say"
bind \ 							"say_team"
bind mouse4 					"+voicerecord"

bind 1 							"slot1"
bind 2 							"slot2"
bind 3 							"slot3"
bind 4 							"slot11"
bind 5 							"slot5"
bind z 							"slot7;"		// flash
bind x 							"slot8" 		// smoke
bind c							"slot6;" 		// nade
bind v							"slot10;" 		// molo
bind n 							"slot9;"		// decoy
bind h 							"slot12"

bind kp_0                       "say bind p say smoked"
bind kp_1                       "say ♿?"
bind kp_2                       "say LJ: 245.1 | 8 Strafes (72%) | 250 / 273 Speed | ✓ W"
bind kp_3                       "say for me it's gg"
bind kp_4                       "say has opened a case and got karambit case hardened (blue gem)"
bind kp_5                       "say ⚠️ THE TROLLGE IS COMING. Seek shelter before The Trollface is released. Any attempt at resistance is futile. Days remaining 🕐: 5"
bind kp_6                       "say stay posted"
bind kp_7                       "say siemanko.cc"
bind kp_8                       "say when bro is lowkirkinely (lowkey + kirk + genuinely) chopped as fuck but he's just took a nonchanichepill (nonchalant + niche + pill) and realizes he's built for ts so he just flowtemaxxes (flow state + maxxing) his preptime (preparation + time) for the GGWII (the second great gloving war)"


host_writeconfig